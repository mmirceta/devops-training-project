name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - run: echo "Hello DevOps"
      - run: echo "Environment loaded: ${{ job.environment }}"