name: Docker-Build/Push Image (Dev)

on:
  push:
    branches:
      - dev
    paths:
      - "docker/**"
      - "!docker/nginx/VERSION"
      - "!docker/apache/VERSION"

permissions:
  contents: read
  id-token: write

jobs:
  build-push:
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ vars.ARM_CLIENT_ID }}
          tenant-id: ${{ vars.ARM_TENANT_ID }}
          subscription-id: ${{ vars.ARM_SUBSCRIPTION_ID }}

      - name: Login to ACR
        run: az acr login --name acrbootstraptraining

      - name: Build nginx image
        run: |
          docker build \
            -f docker/nginx/Dockerfile.nginx \
            -t acrbootstraptraining.azurecr.io/nginx:${{ github.sha }} \
            docker/nginx

      - name: Push nginx image
        run: docker push acrbootstraptraining.azurecr.io/nginx:${{ github.sha }}

      - name: Build apache image
        run: |
          docker build \
            -f docker/apache/Dockerfile.apache \
            -t acrbootstraptraining.azurecr.io/apache:${{ github.sha }} \
            docker/apache

      - name: Push apache image
        run: docker push acrbootstraptraining.azurecr.io/apache:${{ github.sha }}
